# BiliSmooth 工作约定

BiliSmooth 是面向海外 B 站网页观看的 Manifest V3 扩展。改动应服务于实际播放体验、可理解的状态展示和可维护的发布流程。

## 执行与完成

- 根据用户的目标持续完成实现、必要验证和结果交付。提交、推送或发布已在请求范围内时，由智能体执行到底，不把命令或 GitHub 点击步骤转交给用户。
- 按请求区分修改、提交和发布；普通代码修改不自动扩展成公开发布。只有缺失凭据、权限或关键决策确实阻塞时才提出具体问题，同时完成其他可独立工作。
- 先确认工作区、暂存区及分支状态，保留用户和其他智能体的修改。使用明确路径提交；禁止强制推送、移动已发布标签或为整理工作区丢弃未提交内容。
- 完成实现后做一次有范围的集成检查。已通过的检查仅在相关代码再次改变、检查失败或出现新证据时重跑。

## 测试与协作

- 优先复用已有测试。文案、样式和简单配置调整通常不新增前端测试；行为修复只添加能复现具体问题的最少用例。
- 每个任务默认一轮代码审查。发现问题后只复查对应修复，不反复审查整仓，也不追加针对假设风险的防御代码。
- 可并行的工作分配明确文件所有权。信任子代理返回的实现与验证结果，主代理负责整合，避免重复逐行审查和重复运行同一测试。
- 播放结论需区分模拟测试、CDN 分片传输与真实解码播放；记录画质、倍速、时长和限制。短时样本不能写成普遍提速保证。
- 测试结果、测速样本、实验日志、内部迭代记录只保存在已忽略的 `outputs/`、`work/` 或 `archive/`。README、CHANGELOG、Release 正文和安装包文档面向使用者，只写功能、使用方式和用户需要了解的变化，不公开开发过程记录。

## 提交规则

- 提交信息使用英文 Conventional Commit：`type(scope): description`，scope 可省略；正文只说明有助于理解改动的问题、行为和验证。
- **提交标题和正文禁止任何协作者、审查者或工具署名、生成声明及类似尾注，包括 `Co-Authored-By`、`Signed-off-by`、`Reviewed-by`、`Generated-by` 和 `Generated with …`。** 保留 Git 正常作者信息及源码许可证中的真实版权归属。
- 使用项目提交脚本和消息校验器；本地 hook 与 CI 共用校验逻辑。已有 hook 配置需要保留并集成，不能直接覆盖。
- 提交后确认真实提交信息、提交范围和工作区状态。发布任务继续执行标签推送、Actions 跟踪和 Release 附件校验，完成标准是可下载且校验通过的公开 Release。

## 按任务读取

- **提交、版本或发布**：读取 [发布工作流](docs/RELEASING.md)，命令入口见 `package.json`。
- **修改模块职责或跨层行为**：读取 [架构说明](docs/architecture.md)。
- **播放、CDN 或卡顿验证**：读取 [播放验证指南](docs/validation/playback-testing.md)；具体运行结果保留在本地忽略目录。
- **界面视觉改动**：读取 [设计系统](docs/design-system.md)。
- **数据处理、网络访问或来源复用**：核对 [隐私说明](PRIVACY.md) 与 [第三方声明](NOTICE.md)，同步受影响的说明。

只读取当前任务需要的文档。稳定约束留在本文件，具体操作和机械检查留在对应文档与脚本中。
