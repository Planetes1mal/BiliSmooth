# 隐私政策

**中文** | [English](PRIVACY.en.md)

更新日期：2026-09-15

BiliSmooth 是一个独立的 B 站网页播放辅助扩展，用于选择播放线路、显示下载速度和可播余量，以及尝试恢复卡顿。设置和播放分析在你的浏览器中处理。扩展没有开发者运营的数据服务器，不使用广告、分析 SDK 或自动诊断上传。

**本地分析不代表没有网络传输。** 播放和线路探测会连接 B 站及其内容分发服务。接收请求的服务器可看到你的 IP 地址和媒体地址；使用兼容中转时，中转服务还会收到包含签名参数的完整原始媒体地址。

## 处理哪些信息

| 信息 | 用途 |
| --- | --- |
| 已打开的 B 站页面地址、标题，以及当前视频的公共封面 | 在控制面板中识别和选择视频 |
| 播放地址、清单和音视频响应，包括地址中的签名或账号关联参数 | 选择线路并传输当前视频；扩展不会读取登录表单或密码 |
| 播放位置、倍速、分辨率、缓冲、画帧、下载字节和耗时、错误及恢复状态 | 显示播放指标并判断是否需要尝试其他线路 |
| 浏览器语言、时区和可用的连接类型 | 在本地区分线路缓存并响应网络变化；不调用定位 API |
| 播放、外观、浮窗位置和动效设置 | 保存你的偏好 |

扩展通过页面脚本读取以上播放信息，通过浏览器接口识别已打开的 B 站标签页。它不读取浏览器历史记录数据库，不处理其他网站的浏览记录，也不访问支付信息、私人消息、通讯录或本地文件。

## 信息保存在哪里

- **扩展本地存储**：播放和外观设置、动效偏好，以及导入旧设置时留下的备份。它们不会通过扩展同步到云端。
- **B 站页面存储**：启动设置缓存、浮窗位置、线路排名和近期播放反馈。这些数据保存在该站点的 `localStorage`，同一站点的页面脚本也可以读取。这里不保存完整媒体签名地址。
- **页面内存**：当前播放信息、完整媒体地址、请求状态和近期诊断记录。刷新或关闭视频页会释放该页面中的会话数据；控制面板持有的显示副本在面板关闭时释放。已经写入本地存储的设置和缓存另行保留。

设置保留到你修改或删除为止。线路缓存只在有限时间内参与选线，过期不等于立即从磁盘删除。诊断记录使用有数量上限的滚动窗口，较早记录会被替换。扩展没有另行加密本地设置或导出文件，其访问保护依赖浏览器和设备。

## 哪些服务会收到请求

1. **B 站及内容分发网络（CDN）**：播放请求发往原站提供或扩展选择的媒体服务器，主要包括 `bilivideo.com`、`bilivideo.cn`、`bilivideo.net` 和 `akamaized.net` 下的媒体主机。请求保留播放所需的路径和签名参数。原站自身的媒体请求和凭据处理仍受浏览器及 B 站控制。
2. **线路探测服务器**：自动选线和“重新评估网络”会向候选 CDN 下载少量真实媒体数据，因此产生额外流量。探测不携带浏览器 Cookie，但媒体地址本身仍可能包含签名或账号关联参数。
3. **兼容中转服务**：默认情况下，匹配的 MCDN 媒体请求通过 `proxy-tf-all-ws.bilivideo.com` 中转。完整原始媒体 URL（包括查询参数）会作为中转请求的参数发出。你可以在“偏好设置 → 播放兼容性 → 兼容线路处理”中改变这一方式。
4. **封面服务**：控制面板会从 B 站的公共图片服务（如 `hdslb.com`）加载视频封面。

上述服务器会收到正常网络连接信息，包括 IP 地址、请求时间以及浏览器按自身规则发送的请求头；IP 地址可能反映大致区域。扩展选择的新 CDN 和中转地址使用 HTTPS。BiliSmooth 开发者不接收这些播放请求，也不能决定 B 站或 CDN 的日志保留时间。使用扩展不会改变这些服务各自的隐私政策。

## 诊断导出与支持

“导出诊断”只在你的设备上生成 JSON 文件，不会自动发送给开发者。文件包含设置、CDN 主机名、时间、播放状态、性能统计和错误记录；不包含视频标题、封面、页面完整地址或媒体签名 URL。

时间和性能信息仍可能反映观看时段。是否下载、保留或分享文件由你决定。你主动提交的 GitHub 问题及附件由 GitHub 托管，公开问题对所有人可见；不要上传账号凭据、签名媒体地址或其他私人信息。只有你主动提供的支持信息会供项目维护者处理问题。

## 你的控制方式

- 关闭“启用优化”停止后续自动选线、请求改写和自动恢复；刷新视频页以完整应用。播放指标仍可显示。
- “清空记录”删除当前会话已保留的诊断记录。“重新评估网络”重置当前线路排名和近期反馈，并可能开始新的探测。
- “重置播放设置”恢复播放偏好，不会清空全部存储或已经下载的文件。
- 移除扩展并清除其扩展数据，可删除扩展保存的设置。B 站页面存储需要另行通过浏览器站点数据管理清除；这也可能清除 B 站登录状态和其他站点偏好。已导出的文件需要在下载位置删除。
- 如需停止扩展的全部页面处理，可在浏览器中停用或移除扩展，然后刷新已打开的 B 站页面。

## 数据使用承诺

BiliSmooth 将上述信息仅用于其公开说明的播放辅助功能及由你主动请求的故障支持，不出售用户数据，不用于广告、用户画像、信用评估或贷款。扩展对通过 Chrome API 获得的信息的使用遵循 [Chrome Web Store 用户数据政策的 Limited Use 要求](https://developer.chrome.com/docs/webstore/program-policies/limited-use)。

## 联系与更新

隐私问题可以通过 [BiliSmooth 的 GitHub 支持页面](https://github.com/Planetes1mal/BiliSmooth/issues) 联系维护者。该页面是公开渠道，请只提供解决问题所需的非敏感信息。

数据处理方式发生变化时，本政策及相应产品说明会同步更新，并更新上方日期。
